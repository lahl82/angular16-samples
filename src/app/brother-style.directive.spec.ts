import { BrotherStyleDirective } from './brother-style.directive';

describe('BrotherStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BrotherStyleDirective();
    expect(directive).toBeTruthy();
  });
});
